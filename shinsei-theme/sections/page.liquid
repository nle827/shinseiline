{%- comment -%}
  Generic fallback for the "page" template. Dedicated pages (about,
  policy, contact) each have their own template and section
  (page-about.liquid, page-policy.liquid, page-contact.liquid) — this
  file no longer duplicates their content.
{%- endcomment -%}

<h1>{{ page.title }}</h1>
{{ page.content }}

{% schema %}
{
  "name": "t:general.page",
  "settings": []
}
{% endschema %}
